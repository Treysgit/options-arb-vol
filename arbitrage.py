


class Arbitrage:

    @staticmethod
    def put_call_parity(s, p, c, k): 
        min_dif = 0.15

        # Calculate "same payoff"
        left = s + p
        right = c + k

        # Check for arbitrage opportunity
        if 

        